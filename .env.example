# Live SDK verification env file.
# Run `make secrets-setup` to materialize .env.local from these references.

PUTIO_TOKEN_FIRST_PARTY="op://frontend-dev/putio-sdk/first_party/access_token"
PUTIO_CLIENT_ID="op://frontend-dev/putio-sdk/third_party/app_id"
